version: '3.8'
services:
  world-brain:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - brain_data:/app
      - ./brain.db:/app/brain.db  # Persist logs
    restart: unless-stopped

volumes:
  brain_data:
